version: "3.9"

services:
  e-commerce:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8123:8080"
    volumes:
      - /Users/davidhorvat/GolandProjects/e-commerce:/app
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_NAME: "davidhorvat"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "root"
      POSTGRES_DB: "postgres"
  redis:
    image: redis
    ports:
      - "6379:6379"
